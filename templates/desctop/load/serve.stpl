<div page-id="" object-id="" data-link="" data-title="<%= title %>" class="doc_title">
<h4><%= object.name %></h4>
<p style="font-size: 80%;">
  Это опция конкретной технологии и веб-ресурса. Опции бывают
  обязательные, дополнительные, вариативные
  (есть выбор между ними для конкретного случая).
</p>

<% if object.description.is_some() { %>
  <%- object.description.as_deref().unwrap() %>
<% } else { %>
  Описание отсутствует
<% } %>

</div>
